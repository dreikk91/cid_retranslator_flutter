# 🚀 Quick Start Guide - GitHub CI/CD

## Перше налаштування (один раз)

### 1. Закомітьте workflows в репозиторій

```bash
cd /home/apidlypnyi/go_project/cid_retranslator_flutter

# Додайте всі workflow файли
git add .github/
git add .gitignore

# Закомітьте
git commit -m "Add GitHub Actions CI/CD workflows"

# Запуште на GitHub
git push origin main  # або master
```

### 2. Перевірте перший build

1. Відкрийте ваш репозиторій на GitHub
2. Перейдіть у вкладку **Actions**
3. Ви побачите запущений workflow "Build and Release"
4. Клікніть на нього щоб переглянути прогрес

## Щоденне використання

### Просто розробка (без релізу)

```bash
# Робіть зміни в коді
# ...

# Комітьте як зазвичай
git add .
git commit -m "Added new feature"
git push

# Збірка запуститься автоматично!
```

**Результат:** Артефакт буде доступний в Actions → Artifacts

### Створення релізу

```bash
# Коли готові до релізу:
git tag -a v1.0.0 -m "First stable release"
git push origin v1.0.0
```

**Результат:** 
- ✅ Створюється GitHub Release
- ✅ ZIP файл автоматично прикріплюється
- ✅ Генеруються release notes

## Завантаження збірки

### Development build (з Artifacts)

1. GitHub → **Actions**
2. Виберіть останній успішний run
3. Прокрутіть вниз до **Artifacts**
4. Завантажте `cid_retranslator_windows.zip`

### Release build (з Releases)

1. GitHub → **Releases** (права панель)
2. Виберіть потрібну версію
3. В Assets завантажте ZIP

## Приклади команд

### Версіонування (semantic versioning)

```bash
# Patch (1.0.0 → 1.0.1) - bug fixes
git tag -a v1.0.1 -m "Fixed login bug"
git push origin v1.0.1

# Minor (1.0.1 → 1.1.0) - new features
git tag -a v1.1.0 -m "Added dark mode"
git push origin v1.1.0

# Major (1.1.0 → 2.0.0) - breaking changes
git tag -a v2.0.0 -m "Complete redesign"
git push origin v2.0.0
```

### Ручний запуск workflow

1. GitHub → **Actions**
2. Виберіть workflow ліворуч (Build and Release або Build Windows)
3. Клікніть **Run workflow**
4. Виберіть гілку
5. Натисніть зелену кнопку **Run workflow**

### Перегляд логів збірки

```
GitHub → Actions → Виберіть run → Клікніть на job → Розгорніть steps
```

## Що буде в ZIP пакеті?

```
cid_retranslator_v1.0.0_windows.zip
│
├── cid_retranslator.exe          ← Flutter додаток (UI)
├── cid_retranslator_backend.exe  ← Go backend (server)
├── data/                         ← Flutter ресурси
│   ├── flutter_assets/
│   └── ...
├── config.yaml                   ← Конфігурація (якщо є)
├── icon.ico                      ← Іконка Windows
├── icon.png                      ← Іконка PNG
└── README*.md                    ← Документація
```

## Troubleshooting

### ❌ Build failed?

1. Перегляньте логи в Actions
2. Знайдіть червоний крок (failed step)
3. Розгорніть його і читайте помилку

**Типові проблеми:**
- Go код не компілюється → перевірте `main_headless.go`
- Flutter build fails → запустіть локально `flutter build windows`
- Missing dependencies → перевірте `go.mod` та `pubspec.yaml`

### ❌ Release не створюється?

Перевірте:
- Тег починається з `v` (v1.0.0, не 1.0.0)
- Тег запушений: `git push origin v1.0.0`
- У репозиторію Settings → Actions → разрешено створювати releases

### ❌ Артефакт пустий?

Перевірте кожен step в логах:
- Go build успішний?
- Flutter build успішний?
- Package step копіює файли?

## Корисні команди Git

```bash
# Переглянути всі теги
git tag -l

# Видалити тег локально
git tag -d v1.0.0

# Видалити тег на GitHub
git push origin :refs/tags/v1.0.0

# Перенести тег на інший коміт
git tag -fa v1.0.0 -m "Updated tag"
git push origin v1.0.0 --force
```

## Наступні кроки

- ✅ Workflows створені та працюють
- 📝 Прочитайте [workflows/README.md](workflows/README.md) для деталей
- 📊 Перегляньте [WORKFLOW_DIAGRAMS.md](WORKFLOW_DIAGRAMS.md) для візуалізації
- 🎯 Створіть ваш перший реліз!

---

**Потрібна допомога?** Перевірте повну документацію в `workflows/README.md`
